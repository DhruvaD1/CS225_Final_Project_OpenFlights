#include <iostream>
#include <vector>
#include "utils.h"
#include <map>
#include <algorithm>
#include "bfs.h"
#include "parseInput.h"

using namespace std;


int main() {
    string data = file_to_string("data.txt");
    string routes = file_to_string("routes.txt");
    
    // map<string, vector<string>> data_map = parseData(data);

    cout << "check" << endl;
}   

